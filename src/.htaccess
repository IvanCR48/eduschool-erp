# Bloquear acceso directo a código interno
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Compatibilidad con Apache 2.2
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>

# Evitar listado de directorios
Options -Indexes


